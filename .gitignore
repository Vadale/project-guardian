# Rust / Cargo
/target
**/target/
*.rs.bk
# Keep Cargo.lock committed (this workspace builds binaries/apps).

# Local runtime state (never commit user data or secrets)
*.db
*.db-wal
*.db-shm
*.log
/.guardian/
*.local.toml
# Broker secret files — never commit real tokens (keep the *.example.toml demos).
*secrets*.toml
!*secrets*.example.toml
*.key
*.pem
.env
.env.*

# Node / Tauri UI
ui/node_modules/
ui/dist/
ui/.svelte-kit/
ui/src-tauri/target/

# Editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Coverage / fuzz artifacts
/coverage/
fuzz/target/
fuzz/corpus/
fuzz/artifacts/

# Python (evaluation harness)
__pycache__/
*.pyc
.venv/

# Tauri generated code
ui/src-tauri/gen/

# AgentDojo run logs (verbose per-episode traces)
evaluation/agentdojo/runs/
evaluation/agentdojo/runs_gemma/
evaluation/agentdojo/runs_e2b/
evaluation/agentdojo/runs_smoke/
evaluation/agentdojo/runs_smoke2/
# Generated scorecards / scratch run outputs (results live in docs/memory)
evaluation/agentdojo/*.json

# Evaluation run artifacts (operational, not source)
evaluation/**/night/
evaluation/**/logs/
evaluation/**/runs/
evaluation/**/*.log
evaluation/**/__pycache__/
evaluation/inspect/.venv/
