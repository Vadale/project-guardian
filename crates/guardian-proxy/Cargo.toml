[package]
name = "guardian-proxy"
description = "User-space HTTP(S) MITM forward proxy for network policy and provenance (Phase 2)."
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
