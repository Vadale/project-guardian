# Token-broker secrets for the toy-bank demo: `target = "token"`.
# `target` is the upstream's namespace label (here `bank`, from `--upstream bank=...`).
# Guardian injects this token into ALLOWED upstream calls; the agent never sees it.
# In production these live in the OS keychain (Phase 3), not a plaintext file.
bank = "the-bank-token"
